app:
  name: 'GoFlowGateway'
  version: '1.0.0'
  domain_url:

http:
  port: '8080'

postgres:
  pool_max: 10
  url: 'postgres://admin:password@localhost:5432/go_flow_gateway_db?sslmode=disable'

log:
  log_level: 'debug'

redis:
  host: 'localhost'
  port: '6379'
  password: ''

rabbit_mq:
  host: 'localhost'
  port: '5672'
  username: 'guest'
  password: 'guest'

mailhog:
  host: 'localhost'
  port: '1025'
